import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#888888",
    },
    secondary: {
      main: "#f5f5f5",
    },
    tertiary: {
      main: "#dbdbdb",
    },
  },
});

export default theme;
